import webWorker from 'dummy/utils/web-worker';
import { module, test } from 'qunit';

module('Unit | Utility | web worker');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = webWorker();
  assert.ok(result);
});
