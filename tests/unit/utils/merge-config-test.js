import mergeConfig from 'dummy/utils/merge-config';
import { module, test } from 'qunit';

module('Unit | Utility | merge config');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mergeConfig();
  assert.ok(result);
});
