import queryData from 'dummy/utils/query-data';
import { module, test } from 'qunit';

module('Unit | Utility | query data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = queryData();
  assert.ok(result);
});
